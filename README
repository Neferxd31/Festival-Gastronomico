.[!TIP].
En el momento que clonaron el repositorio: 

Para backend Backend (EL ven es un entorno virtual)
cd backend
python -m venv venv   
pip install -r requirements.txt

Para frontend Frontend
cd frontend
npm install
